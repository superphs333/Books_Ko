# 포트폴리오용 트러블 슈팅 (구현 난이도 높은 사례 3선)

## 1) 실시간 채팅 + 이미지 파일 전송에서 메시지 순서/동기화 깨짐

### 문제 상황
- 텍스트/이미지 혼합 채팅에서 이미지 여러 장을 빠르게 전송하면, 수신 측 UI에서 메시지 순서가 꼬이거나 묶음의 시작/끝 구간이 불명확해지는 문제가 발생.

### 원인
- 소켓 기반으로 텍스트(`message`)와 파일(`file`/`files`)을 서로 다른 형태로 전송하면서, 수신 측이 메시지 경계를 명확히 구분하기 어려웠음.
- 다중 이미지 전송 시 각 파일이 “한 묶음의 몇 번째인지” 식별 정보가 없으면 UI에서 그룹 렌더링이 어려움.

### 해결
- 소켓 프로토콜에 `sort`, `order_tag`를 두어 수신 파서가 분기 처리하도록 설계.
- 다중 전송 시 `order_tag`를 0(단일)/1(첫장)/2(중간)/3(마지막)으로 부여해 메시지 묶음의 경계를 명시.
- 수신 스레드에서 JSON 파싱 후 `DataChatting` 모델로 정규화하고, 메인 스레드에서 RecyclerView를 즉시 갱신/하단 스크롤.

### 성과(포트폴리오 문장 예시)
- “소켓 채팅 프로토콜에 `sort + order_tag` 상태값을 도입해 텍스트/다중 이미지 혼합 메시지의 순서 일관성을 확보했고, 실시간 UI 렌더링 안정성을 개선했습니다.”

---

## 2) 메모 이미지 업로드/수정 시 ‘신규 파일 + 기존 서버 파일’ 혼합 처리

### 문제 상황
- 메모 수정 화면에서 기존 서버 이미지와 새로 선택한 로컬 이미지를 함께 다루다 보니, 어떤 이미지를 다시 업로드하고 어떤 이미지는 순서만 유지할지 충돌 발생.

### 원인
- 수정 시점에는 이미지 리스트에 서로 다른 출처(서버 URL/로컬 경로)의 항목이 혼재.
- 단순 일괄 업로드를 하면 불필요한 재업로드가 발생하고, 순서 정보가 보존되지 않음.

### 해결
- 이미지 항목을 순회하며 출처 기준으로 분기:
  - 로컬 파일: Multipart로 업로드하고 신규 파일명 생성.
  - 서버 파일: 재업로드 없이 경로만 유지.
- 별도로 `imgOrderString`을 관리해 최종 순서를 서버에 전달.
- 결과적으로 “업로드 대상”과 “순서 메타데이터”를 분리해 업데이트 정확도 확보.

### 성과(포트폴리오 문장 예시)
- “메모 수정 API를 ‘파일 업로드 파트’와 ‘이미지 순서 메타데이터’로 분리해, 중복 업로드를 줄이고 기존/신규 이미지가 섞인 시나리오에서도 정렬 무결성을 유지했습니다.”

---

## 3) 이미지 편집(밑줄) 기능에서 회전/크기/저장 경로 이슈

### 문제 상황
- 카메라/갤러리 원본 이미지를 커스텀 캔버스에서 편집할 때, 단말에 따라 이미지가 눕거나(Exif 회전), 비율이 깨지거나, 편집본 저장 경로가 달라 후속 업로드가 실패하는 문제가 발생.

### 원인
- 원본 이미지 메타데이터(Exif orientation) 미보정.
- 다양한 해상도 이미지의 리사이즈 정책 부재.
- 편집 완료 파일을 일관된 경로로 반환하지 않으면 상위 화면에서 재사용이 불안정.

### 해결
- Exif orientation을 읽어 실제 각도로 보정 후 비트맵 회전.
- 뷰 크기에 맞춰 비율 기반 리사이즈 후 캔버스 중앙 배치.
- 편집 결과를 캐시 파일로 저장(`Save_Send`)하고 절대 경로를 이전 액티비티로 전달.

### 성과(포트폴리오 문장 예시)
- “커스텀 캔버스 편집 파이프라인에서 Exif 회전 보정·비율 리사이즈·캐시 저장 경로 표준화를 적용해, 단말/입력 소스가 달라도 동일한 편집 결과를 재현하도록 구현했습니다.”

---

## 면접에서 어필 포인트 (짧게)
- **프로토콜 설계 역량**: 단순 기능 구현을 넘어 메시지 경계/순서를 설계한 경험.
- **데이터 무결성 역량**: 파일 바이너리와 메타데이터(순서)를 분리해 문제를 푼 경험.
- **디바이스 다양성 대응**: Exif/URI/저장소 정책 차이를 흡수한 이미지 파이프라인 경험.
